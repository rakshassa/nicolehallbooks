<div class="py-5 pl-4" id="subscribe-floater">
  <h3>Want a FREE book?</h3>
  <p>Sign up for Nicole Hall's weekly newsletter. You’ll get a free ebook just for joining, plus new release info, giveaways, exclusive content, and previews of new books especially for fans.</p>

  <div id="subscribe-contents">
    <%= form_for(@subscription) do |f| %>
      <div class="row">
        <div class="col-12">
          <%= f.label :email, "Email", class: "font-weight-bold" %>
          <div class="input-group input-group-lg">
            <%= f.text_field :email, placeholder: " Your Email Address ",
              class: 'form-control input-lg' %>
          </div>
        </div>
      </div>
      <%= recaptcha_tags %>
      <%= f.submit "Subscribe", class: 'btn my-2', style: 'background: #AF32B2; color: white;' %>
    <% end %>
  </div>
</div>
